services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: light_sentry
      POSTGRES_PASSWORD: light_sentry
      POSTGRES_DB: light_sentry
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
